{
  "buildCommand": "prisma generate && npm run build",
  "installCommand": "npm install",
  "framework": null,
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/app/api/cron/nightly-export",
      "schedule": "0 2 * * *"
    }
  ]
}
